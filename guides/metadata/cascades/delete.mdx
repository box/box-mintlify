---
title: "Delete a metadata cascade policy"
related_endpoints:
  - delete_metadata_cascade_policies_id
related_guides:
  - metadata/cascades/list
  - metadata/cascades/create
related_resources:
  - metadata_cascade_policy
---

import {RelatedLinks, MultiRelatedLinks} from "/snippets/related-links.jsx";

A metadata cascade policy can be deleted by calling the
[`DELETE /metadata_cascade_policies/:id`][e_delete] API endpoint with the
`id` of the policy to remove.

<CodeGroup>
```sh cURL
curl -i -X DELETE "https://api.box.com/2.0/metadata_cascade_policies/324324" \
     -H "authorization: Bearer <ACCESS_TOKEN>"
```

```typescript Node/TypeScript v10
await client.metadataCascadePolicies.deleteMetadataCascadePolicyById(
  cascadePolicyId,
);
```

```python Python v10
client.metadata_cascade_policies.delete_metadata_cascade_policy_by_id(cascade_policy_id)
```

```csharp .NET v10
await client.MetadataCascadePolicies.DeleteMetadataCascadePolicyByIdAsync(metadataCascadePolicyId: cascadePolicyId);
```

```swift Swift v10
try await client.metadataCascadePolicies.deleteMetadataCascadePolicyById(metadataCascadePolicyId: cascadePolicyId)
```

```java Java v10
client.getMetadataCascadePolicies().deleteMetadataCascadePolicyById(cascadePolicyId)
```

```java Java v5
String cascadePolicyID = "e4392a41-7de5-4232-bdf7-15e0d6bba067";
BoxMetadataCascadePolicy policyToDelete = new BoxMetadataCascadePolicy(api, cascadePolicyID);
policyToDelete.delete();
```

```py Python v4
client.metadata_cascade_policy(policy_id='84113349-794d-445c-b93c-d8481b223434').delete()
print('Cascade policy successfully removed')
```

```js Node v4
var policyID = '84113349-794d-445c-b93c-d8481b223434';
client.metadata.deleteCascadePolicy(policyID)
 .then(() => {
  // deletion succeeded — no value returned
 });
```
</CodeGroup>

<Info>

  To get the `id` of the policy,
  [list all policies][g_list_policies] for the folder.

</Info>

[e_delete]: /reference/delete-metadata-cascade-policies-id
[g_list_policies]: /guides/metadata/cascades/list

<RelatedLinks
  title="RELATED APIS"
  items={[
    { label: "Remove metadata cascade policy", href: "/reference/delete-metadata-cascade-policies-id", badge: "DELETE" }
  ]}
/>
<RelatedLinks
  title="RELATED GUIDES"
  items={[
    { label: "List metadata cascade policies", href: "/guides/metadata/cascades/list", badge: "GUIDE" },
    { label: "Create a metadata cascade policy", href: "/guides/metadata/cascades/create", badge: "GUIDE" }
  ]}
/>
