---
title: "Get information about a task"
---

import {RelatedLinks, MultiRelatedLinks} from "/snippets/related-links.jsx";
import {Link} from "/snippets/Link.jsx";

To get information about an assigned task, call the
<Link href="/reference/get-tasks-id">`GET /tasks/:task_id`</Link> API with the
`id` of the task.

<CodeGroup>
```sh cURL
curl -i -X GET "https://api.box.com/2.0/tasks/12345" \
     -H "authorization: Bearer <ACCESS_TOKEN>"
```

```typescript Node/TypeScript v10
await client.tasks.getTaskById(task.id!);
```

```python Python v10
client.tasks.get_task_by_id(task.id)
```

```csharp .NET v10
await client.Tasks.GetTaskByIdAsync(taskId: NullableUtils.Unwrap(task.Id));
```

```swift Swift v10
try await client.tasks.getTaskById(taskId: task.id!)
```

```java Java v10
client.getTasks().getTaskById(task.getId())
```

```java Java v5
BoxTask task = new BoxTask(api, "id");
BoxTask.Info info = task.getInfo();
```

```py Python v4
task = client.task(task_id='12345').get()
print(f'Task ID is {task.id} and the type is {task.type}')
```

```csharp .NET v6
BoxTask task = await client.TasksManager.GetTaskAsync("11111");
```

```js Node v4
client.tasks.get('11111')
 .then(task => {
  /* task -> {
   type: 'task',
   id: '11111',
   item: 
   { type: 'file',
    id: '22222',
    sequence_id: '0',
    etag: '0',
    sha1: '0bbd79a105c504f99573e3799756debba4c760cd',
    name: 'box-logo.png' },
   due_at: '2014-04-03T11:09:43-07:00',
   action: 'review',
   message: 'Please review for publication!',
   task_assignment_collection: { total_count: 0, entries: [] },
   is_completed: false,
   created_by: 
   { type: 'user',
    id: '33333',
    name: 'Example User',
    login: 'user@example.com' },
   created_at: '2013-04-03T11:12:54-07:00' }
  */
 });
```
</CodeGroup>

<RelatedLinks
  title="RELATED APIS"
  items={[
    { label: "Get task", href: "/reference/get-tasks-id", badge: "GET" }
  ]}
/>
<RelatedLinks
  title="RELATED GUIDES"
  items={[
    { label: "Create a task", href: "/guides/tasks/create", badge: "GUIDE" },
    { label: "Lists all tasks for a file", href: "/guides/tasks/for-file", badge: "GUIDE" },
    { label: "Make changes to an existing task", href: "/guides/tasks/update", badge: "GUIDE" }
  ]}
/>
