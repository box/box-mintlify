---
title: "Enterprise構成 (ユーザー設定)"
---

APIバージョン 2025.0

ユーザー設定カテゴリのEnterprise構成。


<ParamField body="enterprise_feature_settings" type="object[]">

<Expandable>
  <ParamField body="is_used" type="boolean">
  例: `true`
  
  指定したEnterpriseに構成が使用されているかどうかを示します。
  </ParamField>

  <ParamField body="value" type="object">
  Enterprise機能の設定。
  
  <Expandable>
    <ParamField body="id" type="string">
    例: `eyJlbnRlcnByaXNlSWQiOiIxMTExNDQyNzY3IiwiZmVhdHVyZUlkIjoiY2FudmFzIn0=`
    
    Enterprise機能設定の識別子。
    </ParamField>
  
    <ParamField body="allowlist" type="object[]">
    Enterprise機能の設定は、このユーザーとグループのセットに対してのみ有効になっています。
    
    <Expandable>
      <ParamField body="id" type="string">
      例: `12345`
      
      ユーザーまたはグループの識別子。
      </ParamField>
    
      <ParamField body="type" type="string">
      例: `user`
      
      種類は`user`または`group`。
      
      次の値のいずれか1つ: `user`,`group`
      </ParamField>
    
    </Expandable>
    </ParamField>
  
    <ParamField body="can_configure" type="boolean">
    例: `true`
    
    機能を構成できるかどうか。
    </ParamField>
  
    <ParamField body="denylist" type="object[]">
    Enterprise機能の設定は、このユーザーとグループのセットを除くすべてのユーザーに対して有効になっています。
    
    <Expandable>
      <ParamField body="id" type="string">
      例: `12345`
      
      ユーザーまたはグループの識別子。
      </ParamField>
    
      <ParamField body="type" type="string">
      例: `user`
      
      種類は`user`または`group`。
      
      次の値のいずれか1つ: `user`,`group`
      </ParamField>
    
    </Expandable>
    </ParamField>
  
    <ParamField body="feature" type="object">
    機能。
    
    <Expandable>
      <ParamField body="id" type="string">
      例: `canvas`
      
      機能の識別子。
      </ParamField>
    
    </Expandable>
    </ParamField>
  
    <ParamField body="is_configured" type="boolean">
    例: `true`
    
    機能が構成されているかどうか。
    </ParamField>
  
    <ParamField body="state" type="string">
    例: `enabled`
    
    機能の状態。
    </ParamField>
  
  </Expandable>
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="is_box_sync_restricted_for_new_users" type="object">
新規ユーザーに対してBox Syncを制限するかどうかを決定します。

<Expandable>
  <ParamField body="is_used" type="boolean">
  例: `true`
  
  指定したEnterpriseに構成が使用されているかどうかを示します。
  </ParamField>

  <ParamField body="value" type="boolean">
  例: `true`
  
  Enterprise構成の値 (ブール値)。
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="is_device_limit_exemption_enabled_for_new_users" type="object">
[デバイス保護] 設定で指定されているデバイスの最大数から新規ユーザーを除外するかどうかを決定します。

<Expandable>
  <ParamField body="is_used" type="boolean">
  例: `true`
  
  指定したEnterpriseに構成が使用されているかどうかを示します。
  </ParamField>

  <ParamField body="value" type="boolean">
  例: `true`
  
  Enterprise構成の値 (ブール値)。
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="is_enterprise_sso_in_testing" type="object">
すべてのユーザーに対してSSOがテストモードかどうかを示す値。

<Expandable>
  <ParamField body="is_used" type="boolean">
  例: `true`
  
  指定したEnterpriseに構成が使用されているかどうかを示します。
  </ParamField>

  <ParamField body="value" type="boolean">
  例: `true`
  
  Enterprise構成の値 (ブール値)。
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="is_enterprise_sso_required" type="object">
すべてのユーザーに対してSSOが必須かどうかを示す値。

<Expandable>
  <ParamField body="is_used" type="boolean">
  例: `true`
  
  指定したEnterpriseに構成が使用されているかどうかを示します。
  </ParamField>

  <ParamField body="value" type="boolean">
  例: `true`
  
  Enterprise構成の値 (ブール値)。
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="is_external_collaboration_restricted_for_new_users" type="object">
新規ユーザーに、外部コラボレータが含まれるフォルダの作成を許可するかどうかを決定します。

<Expandable>
  <ParamField body="is_used" type="boolean">
  例: `true`
  
  指定したEnterpriseに構成が使用されているかどうかを示します。
  </ParamField>

  <ParamField body="value" type="boolean">
  例: `true`
  
  Enterprise構成の値 (ブール値)。
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="is_instant_login_restricted" type="object">
ユーザーがトラブルシューティングのためにBoxサポートへの (制限付き) アクセスを許可することを制限するかどうかを決定します。

<Expandable>
  <ParamField body="is_used" type="boolean">
  例: `true`
  
  指定したEnterpriseに構成が使用されているかどうかを示します。
  </ParamField>

  <ParamField body="value" type="boolean">
  例: `true`
  
  Enterprise構成の値 (ブール値)。
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="is_sso_auto_add_groups_enabled" type="object">
SSOでログインしたユーザー用の新規グループがEnterpriseに登録されていない場合にそのEnterpriseに自動的に追加するかどうかを決定します。

<Expandable>
  <ParamField body="is_used" type="boolean">
  例: `true`
  
  指定したEnterpriseに構成が使用されているかどうかを示します。
  </ParamField>

  <ParamField body="value" type="boolean">
  例: `true`
  
  Enterprise構成の値 (ブール値)。
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="is_sso_auto_add_user_to_groups_enabled" type="object">
SSOでのログイン時にユーザーをEnterpriseグループに追加するかどうかを決定します。

<Expandable>
  <ParamField body="is_used" type="boolean">
  例: `true`
  
  指定したEnterpriseに構成が使用されているかどうかを示します。
  </ParamField>

  <ParamField body="value" type="boolean">
  例: `true`
  
  Enterprise構成の値 (ブール値)。
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="is_sso_auto_remove_user_from_groups_enabled" type="object">
SSOでのログイン時にユーザーをEnterpriseグループから削除するかどうかを決定します。

<Expandable>
  <ParamField body="is_used" type="boolean">
  例: `true`
  
  指定したEnterpriseに構成が使用されているかどうかを示します。
  </ParamField>

  <ParamField body="value" type="boolean">
  例: `true`
  
  Enterprise構成の値 (ブール値)。
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="is_unlimited_storage_enabled_for_new_users" type="object">
新規ユーザーに無制限のストレージを提供するかどうかを決定します。

<Expandable>
  <ParamField body="is_used" type="boolean">
  例: `true`
  
  指定したEnterpriseに構成が使用されているかどうかを示します。
  </ParamField>

  <ParamField body="value" type="boolean">
  例: `true`
  
  Enterprise構成の値 (ブール値)。
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="is_username_change_restricted" type="object">
ユーザーが [アカウント設定] でユーザー名を変更することを制限するかどうかを決定します。

<Expandable>
  <ParamField body="is_used" type="boolean">
  例: `true`
  
  指定したEnterpriseに構成が使用されているかどうかを示します。
  </ParamField>

  <ParamField body="value" type="boolean">
  例: `true`
  
  Enterprise構成の値 (ブール値)。
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="is_view_all_users_enabled_for_new_users" type="object">
新規ユーザーがコラボレータを招待する際に、自分の連絡先に登録されているすべての管理対象ユーザーを表示できるようにするかどうかを決定します。

<Expandable>
  <ParamField body="is_used" type="boolean">
  例: `true`
  
  指定したEnterpriseに構成が使用されているかどうかを示します。
  </ParamField>

  <ParamField body="value" type="boolean">
  例: `true`
  
  Enterprise構成の値 (ブール値)。
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="new_user_default_language" type="object">
例: `{"is_used":true,"value":"English (US)"}`

新規ユーザーのデフォルトの言語を決定します。

<Expandable>
  <ParamField body="is_used" type="boolean">
  例: `true`
  
  指定したEnterpriseに構成が使用されているかどうかを示します。
  </ParamField>

  <ParamField body="value" type="string">
  例: `value of configuration`
  
  Enterprise構成の値 (文字列)。
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="new_user_default_storage_limit" type="object">
新規ユーザー用にデフォルトのストレージの上限を決定します。`is_unlimited_storage_enabled_for_new_users`がtrueの場合は適用されません。

<Expandable>
  <ParamField body="is_used" type="boolean">
  例: `true`
  
  指定したEnterpriseに構成が使用されているかどうかを示します。
  </ParamField>

  <ParamField body="value" type="integer">
  例: `1234`
  
  Enterprise構成の値 (整数)。
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="new_user_default_timezone" type="object">
例: `{"is_used":true,"value":"GMT-05:00 America\\/Chicago CDT"}`

新規ユーザーのデフォルトのタイムゾーンを決定します。

<Expandable>
  <ParamField body="is_used" type="boolean">
  例: `true`
  
  指定したEnterpriseに構成が使用されているかどうかを示します。
  </ParamField>

  <ParamField body="value" type="string">
  例: `value of configuration`
  
  Enterprise構成の値 (文字列)。
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="number_of_user_tracking_codes_remaining" type="object">
作成可能な`user_tracking_codes`の残数。

<Expandable>
  <ParamField body="is_used" type="boolean">
  例: `true`
  
  指定したEnterpriseに構成が使用されているかどうかを示します。
  </ParamField>

  <ParamField body="value" type="integer">
  例: `1234`
  
  Enterprise構成の値 (整数)。
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="user_invites_expiration_time_frame" type="object">
例: `{"is_used":true,"value":"7 days"}`

承認されていないBox Enterpriseアカウントへの招待が期限切れになるまでの期間を決定します。

<Expandable>
  <ParamField body="is_used" type="boolean">
  例: `true`
  
  指定したEnterpriseに構成が使用されているかどうかを示します。
  </ParamField>

  <ParamField body="value" type="string">
  例: `value of configuration`
  
  Enterprise構成の値 (文字列)。
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="user_tracking_codes" type="object">
従業員IDや地域など、追加の値としてユーザーに割り当てられるプロパティ。

<Expandable>
  <ParamField body="is_used" type="boolean">
  例: `true`
  
  指定したEnterpriseに構成が使用されているかどうかを示します。
  </ParamField>

  <ParamField body="value" type="object[]">
  
  <Expandable>
    <ParamField body="id" type="integer(int64)">
    例: `1234`
    
    ユーザー追跡コードのID。
    </ParamField>
  
    <ParamField body="name" type="string">
    例: `Employee ID`
    
    ユーザー追跡コードの名前。
    </ParamField>
  
  </Expandable>
  </ParamField>

</Expandable>
</ParamField>


<ResponseExample>

```json Response Example
{
  "enterprise_feature_settings": [
    {
      "is_used": true,
      "value": {
        "allowlist": [
          {
            "id": "12345",
            "type": "user"
          }
        ],
        "can_configure": true,
        "denylist": [
          {
            "id": "12345",
            "type": "user"
          }
        ],
        "feature": {
          "id": "canvas"
        },
        "id": "eyJlbnRlcnByaXNlSWQiOiIxMTExNDQyNzY3IiwiZmVhdHVyZUlkIjoiY2FudmFzIn0=",
        "is_configured": true,
        "state": "enabled"
      }
    }
  ],
  "is_box_sync_restricted_for_new_users": {
    "is_used": true,
    "value": true
  },
  "is_device_limit_exemption_enabled_for_new_users": {
    "is_used": true,
    "value": true
  },
  "is_enterprise_sso_in_testing": {
    "is_used": true,
    "value": true
  },
  "is_enterprise_sso_required": {
    "is_used": true,
    "value": true
  },
  "is_external_collaboration_restricted_for_new_users": {
    "is_used": true,
    "value": true
  },
  "is_instant_login_restricted": {
    "is_used": true,
    "value": true
  },
  "is_sso_auto_add_groups_enabled": {
    "is_used": true,
    "value": true
  },
  "is_sso_auto_add_user_to_groups_enabled": {
    "is_used": true,
    "value": true
  },
  "is_sso_auto_remove_user_from_groups_enabled": {
    "is_used": true,
    "value": true
  },
  "is_unlimited_storage_enabled_for_new_users": {
    "is_used": true,
    "value": true
  },
  "is_username_change_restricted": {
    "is_used": true,
    "value": true
  },
  "is_view_all_users_enabled_for_new_users": {
    "is_used": true,
    "value": true
  },
  "new_user_default_language": {
    "is_used": true,
    "value": "English (US)"
  },
  "new_user_default_storage_limit": {
    "is_used": true,
    "value": 1234
  },
  "new_user_default_timezone": {
    "is_used": true,
    "value": "GMT-05:00 America\\/Chicago CDT"
  },
  "number_of_user_tracking_codes_remaining": {
    "is_used": true,
    "value": 1234
  },
  "user_invites_expiration_time_frame": {
    "is_used": true,
    "value": "7 days"
  },
  "user_tracking_codes": {
    "is_used": true,
    "value": [
      {
        "id": 1234,
        "name": "Employee ID"
      }
    ]
  }
}
```

</ResponseExample>
