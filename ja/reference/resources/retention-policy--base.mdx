---
title: "リテンションポリシー (Base)"
---

APIバージョン 2024.0

<Note>
このリソースは、バージョン**2024.0**のエンドポイントで使用されています。
詳細については、
**[Box APIのバージョン管理](/ja/guides/api-calls/api-versioning-strategy/)**を参照してください。

「[Box SDKのバージョニング戦略](/ja/guides/tooling/sdks/sdk-versioning/)について詳しく学ぶ。」
</Note>

リテンションポリシーのBase版の表示。

* [Standard](/ja/reference/resources/retention-policy/)
* [Mini](/ja/reference/resources/retention-policy--mini/)
* [Base](/ja/reference/resources/retention-policy--base/)

<Info>
このリソースにはAPIの使用時に発生する可能性があるいくつかのバリエーションがあります。

**Base**バリアントに含まれるフィールドは、`fields`クエリパラメータで指定されたフィールドのほかに、そのパラメータを使用しているときに必ず返されるデフォルトのフィールドを表します。
</Info>

<ParamField body="id" type="string">
例 `12345`

リテンションポリシーを表す一意の識別子。
</ParamField>

<ParamField body="type" type="string">
例 `retention_policy`

値は常に`retention_policy`になります。

次の値に固定: `retention_policy`
</ParamField>

<ResponseExample>

```json レスポンスの例
{
  "id": "12345",
  "type": "retention_policy"
}
```

</ResponseExample>
