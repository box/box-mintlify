---
title: "コメント (Base)"
---

APIバージョン 2024.0

コメントのBase版の表示。

* [Full](/ja/reference/resources/comment--full/)
* [Standard](/ja/reference/resources/comment/)
* [Base](/ja/reference/resources/comment--base/)

<Info>
このリソースにはAPIの使用時に発生する可能性があるいくつかのバリエーションがあります。

**Base**バリアントに含まれるフィールドは、`fields`クエリパラメータで指定されたフィールドのほかに、そのパラメータを使用しているときに必ず返されるデフォルトのフィールドを表します。
</Info>

<ParamField body="id" type="string">
例 `11446498`

このコメントの一意の識別子。
</ParamField>

<ParamField body="type" type="string">
例 `comment`

値は常に`comment`になります。

次の値に固定: `comment`
</ParamField>

<ResponseExample>

```json レスポンスの例
{
  "id": "11446498",
  "type": "comment"
}
```

</ResponseExample>
