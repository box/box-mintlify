---
hide_in_page_nav: true
sidebarTitle: 2. Configure a Box App
title: Boxアプリの設定
type: quick-start

fullyTranslated: true
---

import {Link} from "/snippets/Link.jsx";

import { Choose, Choice, Grid, Trigger, Observe, ChoiceDebug } from '/snippets/choose-choice-components.jsx';
import { Store } from '/snippets/store.jsx';
import { AppButton } from '/snippets/app-button.jsx';
import { ProgressBar } from '/snippets/progress-bar.jsx';

import {RelatedLinks, MultiRelatedLinks} from "/snippets/related-links.jsx";

<ProgressBar
  pages={[
                        "guides/tooling/postman/quick-start/install-postman",
                        "guides/tooling/postman/quick-start/configure-box-app",
                        "guides/tooling/postman/quick-start/log-in-to-box",
                        "guides/tooling/postman/quick-start/load-postman-collection",
                        "guides/tooling/postman/quick-start/make-api-call",
                        "guides/tooling/postman/quick-start/next-steps"
                      ]}
/>

**Postmanコレクション**を使用するために、Postmanアプリケーションは**アクセストークン**を使用してBox APIから認証を受ける必要があります。アクセストークンを取得するには、**Boxアプリ**を使用してBoxにログインする方法が最も簡単です。

**Boxアプリ**は、APIコールの実行に使用できるアプリケーションです。**Postmanコレクション**を使用する際は、独自のBoxアプリを設定するか、あらかじめ設定されているBoxアプリを使用するかを選択できます。独自のBoxアプリを設定した場合の主な利点は、1時間ごとにログインする必要がなくなることです。ただし、設定にいくつか追加手順が必要になります。

## 使用するBoxアプリの選択

<Grid columns="2">
  <Choose option="postman.app_type" value="create_new" color="blue">
    # 新しいBoxアプリを作成する

    まだBoxアプリを作成していない場合や追加のアプリを作成したい場合は、この手順に従います。
  </Choose>

  <Choose option="postman.app_type" value="use_existing" color="blue">
    # 既存のアプリを使用する

    使用したいBoxアプリをすでに作成済みの場合は、そのアプリケーションの資格情報を使用できます。
  </Choose>
</Grid>

<Choice option="postman.app_type" value="create_new,clicked" color="blue">
  # Boxアプリの作成

  独自のBoxアプリを使用するには、**Box開発者コンソール**で新しいBoxアプリを作成する必要があります。次の例では、OAuth2.0を使用していますが、他の認証方法も使用可能です。

  1. [開発者コンソール][devconsole]に移動します。
  2. \[**Platformアプリの作成**] を選択します。
  3. \[**アプリ名**] を入力し、\[**アプリタイプ**] として \[**OAuth 2.0**] を選択します。
  4. \[**構成**] タブで、\[**OAuth 2.0リダイレクトURI**] セクションまで下にスクロールし、\[**リダイレクトURI**] に値`https://developer.box.com/auth/callback`を設定します。`box.dev`でこのチュートリアルにアクセスした場合は \[**リダイレクトURI**] が`https://box.dev/auth/callback`になることに注意してください。
  5. \[**アプリケーションスコープ**] セクションまでスクロールし、目的の<Link href="/guides/api-calls/permissions-and-errors/scopes">スコープ</Link>を選択します。**アプリケーションには、** **次のスコープの1つ以上が必要です:** ユーザーを管理する、Boxに格納されているすべてのファイルとフォルダの読み取り、Boxに格納されているすべてのファイルとフォルダの読み取りと書き込み
  6. ページ上部にある \[**変更を保存**] ボタンをクリックします。
  7. \[**OAuth 2.0資格情報**] セクションから、\[**クライアントID**] および \[**クライアントシークレット**] の値をコピーします。これらの資格情報は、次の手順でアプリケーションを認証する際に使用します。
</Choice>

{/*
  <Trigger option="postman.app_type" value="clicked">
    <AppButton
      id='postman'
      name='Postman'
      scopes='root_readonly,root_readwrite,manage_managed_users,manage_app_users,manage_groups,manage_webhook,manage_enterprise_properties,manage_data_retention,item_execute_integration'
      can_act_as_user
      authentication_type='auth_code_grant'
      redirect_url='/auth/callback'
      cors_origins=''>
      Create an app
    </AppButton>
  </Trigger>

  <Observe option="postman.app_type" value="clicked">
    We will use these credentials to authenticate your application in the next
    step.
  </Observe>
  */}

<Choice option="postman.app_type" value="use_existing" color="blue">
  # 既存のアプリを使用する

  Boxアプリをすでに作成済みの場合は、それを使用することも可能です。そのアプリを使用するには、必要な設定がいくつかあります。次の例では、OAuth2.0を使用していますが、他の認証方法も使用可能です。

  1. [開発者コンソール][devconsole]に移動します。
  2. アプリケーションを選択します。
  3. アプリの \[**構成**] タブに移動します。
  4. アプリケーションで認証方法として**標準OAuth 2.0**を使用していることを確認します。
  5. \[**OAuth 2.0リダイレクトURI**] セクションまで下にスクロールし、\[**リダイレクトURI**] に値`https://developer.box.com/auth/callback`を設定します。`box.dev`でこのチュートリアルにアクセスした場合は \[**リダイレクトURI**] が`https://box.dev/auth/callback`になることに注意してください。
  6. \[**アプリケーションスコープ**] セクションまでスクロールし、目的の<Link href="/guides/api-calls/permissions-and-errors/scopes">権限</Link>を選択します。**アプリケーションには、** **次のスコープの1つ以上が必要です:** ユーザーを管理する、Boxに格納されているすべてのファイルとフォルダの読み取り、Boxに格納されているすべてのファイルとフォルダの読み取りと書き込み
  7. ページ上部にある \[**変更を保存**] ボタンをクリックします。
  8. \[**OAuth 2.0資格情報**] セクションから、\[**クライアントID**] および \[**クライアントシークレット**] の値をコピーします。これらの資格情報は、次の手順でアプリケーションを認証する際に使用します。
</Choice>

{/*
  <Store id='postman_credentials.client_id' placeholder='zECq2EkYBjZ...' pattern='\w{32}'>
    Client ID
  </Store>

  <Store id='postman_credentials.client_secret' placeholder='913td9hr6jo...' pattern='\w{32}'>
    Client Secret
  </Store>

  We will use these credentials to authenticate your application in the next
  step.


  <Choice option='postman.app_type' value='create_new,use_existing,clicked' color='none'>

  <Danger>
  **Security notice**

  Your API credentials are now stored in the browser cache. We highly
  recommend clearing out this storage by clicking the **Reset** button later in
  this guide.
  </Danger>

  </Choice>

  <Choice option='postman.app_type' value='create_new,use_existing,clicked' color='none'>

  ## Summary

  * You either selected to create a new **Box App**
    * Signed up for a developer account (Optional)
    * Had us create **Platform App** for you that uses **OAuth 2.0** authentication
    * Had us set up the **redirect URL** for the application
  * Or you selected to use an **existing Box App**

  </Choice>
  */}

## まとめ

* **Boxアプリ**の新規作成を選択し、手順に従って**クライアントID**と**クライアントシークレット**を取得しました
* または、**既存のBoxアプリ**の使用を選択し、手順に従って**クライアントID**と**クライアントシークレット**を取得しました

<Observe option="postman.app_type" value="create_new,use_existing,clicked">
  <Next>Boxアプリの設定が完了しました</Next>
</Observe>

[devconsole]: https://cloud.app.box.com/developers/console

[signup]: https://account.box.com/signup/n/developer

<RelatedLinks
  title="関連するガイド"
  items={[
  { label: "Platform App", href: "/guides/applications/app-types/platform-apps", badge: "GUIDE" }
]}
/>
