---
title: "ファイルリクエストに関する情報の取得"
related_endpoints:
  - get_file_requests_id
related_guides:
  - file-requests/copy
required_guides: []
related_resources:
  - file_request
category_id: file-requests
subcategory_id: null
is_index: false
id: file-requests/get
rank: 3
type: guide
total_steps: 5
sibling_id: file-requests
parent_id: file-requests
next_page_id: file-requests/update
previous_page_id: file-requests/copy
source_url: >-
  https://github.com/box/developer.box.com/blob/main/content/guides/file-requests/3-get.md
fullyTranslated: true
---

import {RelatedLinks, MultiRelatedLinks} from "/snippets/related-links.jsx";

既存の[ファイルリクエスト](/ja/guides/file-requests/template)の詳細を取得するのに必要なのは、その一意のIDだけです。

<CodeGroup>
```sh cURL
curl -i -X GET "https://api.box.com/2.0/file_requests/42037322" \
     -H "authorization: Bearer <ACCESS_TOKEN>"
```

```typescript Node/TypeScript v10
await client.fileRequests.getFileRequestById(fileRequestId);
```

```python Python v10
client.file_requests.get_file_request_by_id(file_request_id)
```

```csharp .NET v10
await client.FileRequests.GetFileRequestByIdAsync(fileRequestId: fileRequestId);
```

```swift Swift v10
try await client.fileRequests.getFileRequestById(fileRequestId: fileRequestId)
```

```java Java v10
client.getFileRequests().getFileRequestById(fileRequestId)
```

```java Java v5
BoxFileRequest fileRequest = new BoxFileRequest(api, "id");
BoxFileRequest.Info fileRequestInfo = fileRequest.getInfo();
```

```py Python v4
file_request = client.file_request(file_request_id='123456').get()
print(f'File request {file_request.id} on folder {file_request.folder.name}')
```

```csharp .NET v6
BoxFileRequestObject fileRequest = await client.FileRequestsManager.GetFileRequestByIdAsync("12345");
```

```js Node v4
client.fileRequests.getById(fileRequestId)
```
</CodeGroup>


<Note>

ファイルリクエストのIDを確認するには、BoxウェブアプリにアクセスしてそのURLを調べます。ファイルリクエストテンプレートの設定に関する[ガイド](/ja/guides/file-requests/template)で、ファイルリクエストIDの確認方法を確認してください。

</Note>

<RelatedLinks
  title="関連するAPI"
  items={[
    { label: "Get file request", href: "/ja/reference/get-file-requests-id", badge: "GET" }
  ]}
/>
<RelatedLinks
  title="関連するガイド"
  items={[
    { label: "ファイルリクエストのコピー", href: "/ja/guides/file-requests/copy", badge: "ガイド" }
  ]}
/>
