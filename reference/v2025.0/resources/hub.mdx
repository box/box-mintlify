---
title: "Box Hub"
---

API version 2025.0

A standard representation of a Box Hub, as returned from any
Box Hubs API endpoints by default.

* [Standard](/reference/v2025.0/resources/hub/)
* [Base](/reference/v2025.0/resources/hub--base/)

<Info>
This resource has a few variations that can be encountered when
using the API.

The fields that are part of the **standard** variant are generally
returned when the resource is requested via its own API endpoints.
For example, when retrieving a file by ID, it will return
these fields unless the `fields` parameter has been specified.
</Info>

<ParamField body="id" type="string">
example: `12345`

The unique identifier that represent a Box Hub.

The ID for any Box Hub can be determined
by visiting a Box Hub in the web application
and copying the ID from the URL. For example,
for the URL `https://*.app.box.com/hubs/123`
the `hub_id` is `123`.
</ParamField>

<ParamField body="type" type="string">
example: `hubs`

The value will always be `hubs`.

Value is always `hubs`
</ParamField>

<ParamField body="can_non_owners_invite" type="boolean">
example: `true`

Indicates if non-owners can invite others to the Box Hub.
</ParamField>

<ParamField body="can_shared_link_be_created" type="boolean">
example: `true`

Indicates if a shared link can be created for the Box Hub.
</ParamField>

<ParamField body="created_at" type="string(date-time)">
example: `2012-12-12T10:53:43Z`

The date and time when the folder was created. This value may
be `null` for some folders such as the root folder or the trash
folder.
</ParamField>

<ParamField body="created_by" type="object">
The user who created this Box Hub.

<Expandable>
  <ParamField body="id" type="string">
  example: `11446498`
  
  The unique identifier for this user.
  </ParamField>

  <ParamField body="type" type="string">
  example: `user`
  
  The value will always be `user`.
  
  Value is always `user`
  </ParamField>

  <ParamField body="login" type="string(email)">
  example: `ceo@example.com`
  
  The primary email address of this user.
  </ParamField>

  <ParamField body="name" type="string">
  example: `Aaron Levie`
  
  The display name of this user.
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="description" type="string">
example: `All the contracts for the company.`

The description of the Box Hub. First 200 characters are returned.
</ParamField>

<ParamField body="is_ai_enabled" type="boolean">
example: `true`

Indicates if AI features are enabled for the Box Hub.
</ParamField>

<ParamField body="is_collaboration_restricted_to_enterprise" type="boolean">
example: `true`

Indicates if collaboration is restricted to the enterprise.
</ParamField>

<ParamField body="title" type="string">
example: `Contracts`

The title given to the Box Hub.
</ParamField>

<ParamField body="updated_at" type="string(date-time)">
example: `2012-12-12T10:53:43Z`

The date and time when the Box Hub was last updated.
</ParamField>

<ParamField body="updated_by" type="object">
The user who last modified this Box Hub.

<Expandable>
  <ParamField body="id" type="string">
  example: `11446498`
  
  The unique identifier for this user.
  </ParamField>

  <ParamField body="type" type="string">
  example: `user`
  
  The value will always be `user`.
  
  Value is always `user`
  </ParamField>

  <ParamField body="login" type="string(email)">
  example: `ceo@example.com`
  
  The primary email address of this user.
  </ParamField>

  <ParamField body="name" type="string">
  example: `Aaron Levie`
  
  The display name of this user.
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="view_count" type="integer(int32)">
example: `506`

The number of views for the Box Hub.
</ParamField>

<ResponseExample>

```json Response Example
{
  "id": "12345",
  "type": "hubs",
  "can_non_owners_invite": true,
  "can_shared_link_be_created": true,
  "created_at": "2012-12-12T10:53:43Z",
  "created_by": {
    "id": "11446498",
    "type": "user",
    "login": "ceo@example.com",
    "name": "Aaron Levie"
  },
  "description": "All the contracts for the company.",
  "is_ai_enabled": true,
  "is_collaboration_restricted_to_enterprise": true,
  "title": "Contracts",
  "updated_at": "2012-12-12T10:53:43Z",
  "updated_by": {
    "id": "11446498",
    "type": "user",
    "login": "ceo@example.com",
    "name": "Aaron Levie"
  },
  "view_count": 506
}
```

</ResponseExample>
