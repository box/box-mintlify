---
title: "Invite"
---

API version 2024.0

<Note>
This endpoint is in the version **2024.0**. No changes are required to continue using it. For more details, see **[Box API versioning](/guides/api-calls/api-versioning-strategy)**. 

Learn more about [Box SDK versioning strategy](/guides/tooling/sdks/sdk-versioning/).
</Note>

An invite for a user to an enterprise.

<ParamField body="id" type="string">
example: `11446498`

The unique identifier for this invite.
</ParamField>

<ParamField body="type" type="string">
example: `invite`

The value will always be `invite`.

Value is always `invite`
</ParamField>

<ParamField body="actionable_by" type="User (Mini)object">
A mini representation of a user, as can be returned when nested within other
resources.
</ParamField>

<ParamField body="created_at" type="string(date-time)">
example: `2012-12-12T10:53:43-08:00`

When the invite was created.
</ParamField>

<ParamField body="invited_by" type="User (Mini)object">
A mini representation of a user, as can be returned when nested within other
resources.
</ParamField>

<ParamField body="invited_to" type="object">
A representation of a Box enterprise.

<Expandable>
  <ParamField body="id" type="string">
  example: `11446498`
  
  The unique identifier for this enterprise.
  </ParamField>

  <ParamField body="type" type="string">
  example: `enterprise`
  
  The value will always be `enterprise`.
  
  Value is always `enterprise`
  </ParamField>

  <ParamField body="name" type="string">
  example: `Acme Inc.`
  
  The name of the enterprise.
  </ParamField>

</Expandable>
</ParamField>

<ParamField body="modified_at" type="string(date-time)">
example: `2012-12-12T10:53:43-08:00`

When the invite was modified.
</ParamField>

<ParamField body="status" type="string">
example: `pending`

The status of the invite.
</ParamField>

<ResponseExample>

```json Response Example
{
  "id": "11446498",
  "type": "invite",
  "actionable_by": {
    "id": "11446498",
    "type": "user",
    "login": "ceo@example.com",
    "name": "Aaron Levie"
  },
  "created_at": "2012-12-12T10:53:43-08:00",
  "invited_by": {
    "id": "11446498",
    "type": "user",
    "login": "ceo@example.com",
    "name": "Aaron Levie"
  },
  "invited_to": {
    "id": "11446498",
    "type": "enterprise",
    "name": "Acme Inc."
  },
  "modified_at": "2012-12-12T10:53:43-08:00",
  "status": "pending"
}
```

</ResponseExample>
